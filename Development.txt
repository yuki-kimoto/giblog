# Development

# Create new website
rm -rf mysite && make && perl -Mblib script/giblog new mysite
rm -rf mysite2 && make && perl -Mblib script/giblog new mysite2

# Create new entry
make && perl -Mblib script/giblog --giblog-dir=mysite add

# Build
make && perl -Mblib script/giblog --giblog-dir=mysite build

# Serve
make && mysite/serve

make && perl -Mblib script/giblog --giblog-dir=mysitezemi build
