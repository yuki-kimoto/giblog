# Development

# Create new website
make && perl -Mblib script/giblog new mysite1
make && perl -Mblib script/giblog new mysite2
make && perl -Mblib script/giblog new mysite3
make && perl -Mblib script/giblog new mysite5

# Create new entry
make && perl -Mblib script/giblog --giblog-dir=mysite add
make && perl -Mblib script/giblog --giblog-dir=mysite4 add

# Add index content

# Build
make && perl -Mblib script/giblog --giblog-dir=mysite build
make && perl -Mblib script/giblog --giblog-dir=mysite4 build
