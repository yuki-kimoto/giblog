# Development

# Create new website
make && perl -Mblib script/giblog new_website mysite1
make && perl -Mblib script/giblog new_website mysite2
make && perl -Mblib script/giblog new_website mysite3

# Create new entry
make && perl -Mblib script/giblog --giblog-dir=mysite new_entry
make && perl -Mblib script/giblog --giblog-dir=mysite4 new_entry

# Add index content

# Build
make && perl -Mblib script/giblog --giblog-dir=mysite build
