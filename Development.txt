# Development

# Create new website
make && perl -Mblib script/giblog new mysite1

# Create new entry
make && perl -Mblib script/giblog --giblog-dir=mysite1 add

# Build
make && perl -Mblib script/giblog --giblog-dir=mysite1 build
